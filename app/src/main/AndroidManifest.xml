<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roushan.verboseassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BuyerSellerNavActivity"
            android:label="@string/title_activity_buyer_seller_nav"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BuyerRegisterActivity" />
        <activity android:name=".SellerRegisterActivity" />
        <activity android:name=".SelectBuyerSeller"></activity>
    </application>

</manifest>